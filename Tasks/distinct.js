// Return an array without duplicates
"use strict";

const distinct = (array) => {
  console.log({ array });
  array.forEach((a) => {
    if () {
      
    } else {
      
    }
    
  });
};

require('../Tests/distinct.js')(distinct);