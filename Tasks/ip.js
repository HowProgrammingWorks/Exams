'use strict'

const parseIp = ip => {
  const value = [];
  const splitArr = ip.split('.');
  j = 0
    for ( const b of B) {
  a[j] = parseInt(b);
  j++;
  }};
  return a;
};

const result = Parseip('127.0.0.1')
console.log(result)
