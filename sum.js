// Sum all numbers from array
'use strict'
const sum = (s) => {
  for (i of s)
  {
  let t = typeof i;
  if (t === 'number'){
    
    }
  }
  };

const result = sum([5, true, 'string', 7, 'hello']);
console.log(result);
